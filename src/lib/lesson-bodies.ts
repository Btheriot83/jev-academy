export const LESSON_BODIES: Record<string, string> = {
  "what-system-one-is": "## The point\n\nLLMs write text for humans to read. When your **code** needs a decision, digging an answer out of free-form text is the wrong tool.\n\n**Jev** is TypeSafe's flagship **System One** model. You send it **state** plus typed **questions**. You get back structured answers and probabilities. Your software can branch on them, sort them, and route with them. No text generation. No parsing.\n\nHere is a simple way to picture it. An LLM writes you a letter. Jev fills out a form.\n\n## How Jev compares to an LLM\n\n- An LLM gives you generated text. Jev gives you typed decisions plus probabilities.\n- With an LLM, you write a prompt, then parse JSON out of the reply. With Jev, the answer is structured from the start.\n- LLMs fit chat, generation, and open-ended reasoning. Jev fits fast, narrow judgment calls inside a workflow.\n\n## What Jev does not do\n\nJev does not write replies, generate code, or explain its reasoning. You define the space of possible answers yourself, using the primitives: Choice (pick one option from a set), Noul (yes or no, with a probability), and Score (rate something on a fixed scale).\n\n## What goes in\n\nInput is text only. That can be a plain string, a JSON object, or an array of text. Images, audio, and video are not supported yet. English is the primary language.\n\n> Name note: \"System One\" echoes Kahneman's fast System 1. The emphasis is on focused judgments. Details in [System One](https://docs.typesafe.ai/concepts/system-one.md).\n\nTraining framing from TypeSafe's docs: **RLCD** means reinforcement learning for calibrated decisions. Generative models use RLHF or RLVR instead. More in the [AI primer](https://docs.typesafe.ai/introduction/machine-learning-primer.md).\n\n### Next\n\nMove on to **When to use Jev, an LLM, or plain code**. Then verify your key.",
  "when-to-use-jev": "## The big rule\n\nBuild AI-powered software, not self-running agents. **Your code owns the control flow.** You drop System One in wherever you need judgment calls on unstructured data that plain code cannot make. TypeSafe lays this out in [How to build](https://docs.typesafe.ai/concepts/how-to-build-with-system-one.md).\n\nPicture a sports game. An LLM is the sportswriter who writes a long story about it afterward. Jev is the referee who snaps out fast, narrow calls all game long. Your workflow wants the referee.\n\n## Use Jev when you need\n\n- Routing or classification into a fixed set of options, with probabilities\n- A yes/no check on meaning, with a usable P(true)\n- Rubric scores you can threshold or sort\n- Many small, atomic judgments in parallel over the same state\n- Cheap verification or guardrails around other AI\n- Fast intent routing before an expensive LLM call\n\n## Keep it in code when\n\nThe work is deterministic: fixed rules, arithmetic, counting, date ordering, regex or parser extraction, combining weights, escalating.\n\n## Use a generative LLM when\n\nYou need free-form text, multi-hop generation, or open-ended writing. Generation is not Jev's job. See jaggedness.\n\n## The design loop\n\nThe docs give an eight-step loop:\n\n1. Keep deterministic work in code\n2. Cut state down to only what the questions need\n3. Prefer structured JSON state\n4. Ask atomic questions\n5. Structure instructions and criteria when a question has multiple parts\n6. Fan out independent questions in one request\n7. Combine the answers in code\n8. Gate on confidence: act, review, or escalate\n\n### Next\n\nVerify `TYPESAFE_API_KEY` without printing it.",
  "credentials": "## Keep your key safe\n\nTreat your API key like a house key. Anyone holding it can walk in. So you never leave copies lying around.\n\n1. Create a key in the TypeSafe dashboard: [console keys](https://console.typesafe.ai/settings/keys).\n2. Store it in one place only: the environment variable **`TYPESAFE_API_KEY`**.\n3. For raw HTTP, send `Authorization: Bearer $TYPESAFE_API_KEY`.\n4. The SDKs read `TYPESAFE_API_KEY` by default.\n5. Never commit keys, never put them in client-side pages, and never invent placeholder keys that look real. The JS SDK's `dangerouslyAllowBrowser` defaults to false. Do not turn it on for public lessons.\n\nSources: [Quick start](https://docs.typesafe.ai/introduction/quickstart.md), [CORE credentials](https://docs.typesafe.ai/concepts/system-one.md).\n\n## Verify without showing the secret\n\nFail closed. Print the key's **length** or the **HTTP status**. Never print the key value.\n\n```bash\n# Length only \u2014 never echo the key\nif [ -z \"$TYPESAFE_API_KEY\" ]; then\n  echo \"TYPESAFE_API_KEY is not set\"\n  exit 1\nfi\necho \"TYPESAFE_API_KEY length: ${#TYPESAFE_API_KEY}\"\n\n# Auth test \u2014 print status only (401 = bad/missing key)\ncode=$(curl -s -o /dev/null -w \"%{http_code}\"   -H \"Authorization: Bearer $TYPESAFE_API_KEY\"   https://api.typesafe.ai/v1/models)\necho \"GET /v1/models => HTTP $code\"\n# Expect 200 when the key is valid (docs: models endpoint uses Bearer auth)\n```\n\n```js\nconst key = process.env.TYPESAFE_API_KEY;\nif (!key) throw new Error(\"TYPESAFE_API_KEY is not set\");\nconsole.log(\"key length:\", key.length); // never console.log(key)\n\nconst res = await fetch(\"https://api.typesafe.ai/v1/models\", {\n  headers: { Authorization: `Bearer ${key}` },\n});\nconsole.log(\"status\", res.status); // do not log Authorization header\n```\n\n### Next\n\nMake your first `POST /v1/systemone` call.",
  "first-api-call": "## The endpoint\n\n```http\nPOST https://api.typesafe.ai/v1/systemone\nAuthorization: Bearer <API_KEY>\nContent-Type: application/json\n```\n\nThe body needs three fields: `state`, `model` (for example `\"jev-latest\"`), and `questions` (a named map). Errors you might hit: 401, 422, 429, or 529. See [API](https://docs.typesafe.ai/api.md).\n\nThink of this call like a doctor's visit. `state` is why you showed up. `questions` is the checklist the doctor runs. Here the state is a frustrated customer message, and the checklist has one item: a Noul asking \"Does this message express urgency?\" A Noul answers yes or no, plus a probability.\n\n## curl (from Quick start)\n\n```bash\ncurl -X POST https://api.typesafe.ai/v1/systemone \\\n  -H \"Authorization: Bearer $TYPESAFE_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n  \"state\": \"Hi, I have been trying to connect my Stripe account for 3 days and it keeps failing. I am losing sales. Please help ASAP.\",\n  \"model\": \"jev-latest\",\n  \"questions\": {\n    \"urgency\": {\n      \"type\": \"noul\",\n      \"instructions\": \"Does this message express urgency?\"\n    }\n  }\n}'\n```\n\n## JavaScript SDK\n\n```ts\nimport { noul, TypeSafeClient } from \"@typesafe-ai/sdk\";\n\nconst client = new TypeSafeClient(); // reads TYPESAFE_API_KEY\nconst response = await client.systemOne({\n  state: \"Hi, I have been trying to connect my Stripe account for 3 days and it keeps failing. I am losing sales. Please help ASAP.\",\n  questions: {\n    urgency: noul(\"Does this message express urgency?\"),\n  },\n});\nconsole.log(response.answers.urgency.noul);\n```\n\nInstall: `npm install @typesafe-ai/sdk` (Node 20+).\n\n## Python SDK\n\n```python\nfrom typesafe_sdk import Noul, TypeSafeClient\n\nwith TypeSafeClient() as client:\n    response = client.system_one(\n        state=\"Hi, I have been trying to connect my Stripe account for 3 days and it keeps failing. I am losing sales. Please help ASAP.\",\n        questions={\n            \"urgency\": Noul(instructions=\"Does this message express urgency?\"),\n        },\n    )\nprint(response.nouls[\"urgency\"].noul)\n# Also: response.answers[\"urgency\"]\n```\n\nInstall: `pip install typesafe-sdk` (Python 3.10 or newer).\n\n### Next\n\nLearn **State craft**, then run the Choice, Noul, and Score labs.",
  "state": "## What State is\n\n**State** is the material you want judged. It might be a message, an article, an app snapshot, or records plus a policy.\n\nEach request has one state. Every question sees that same state, and each question is judged on its own. ([State](https://docs.typesafe.ai/concepts/state.md))\n\nThink of State like a folder you hand to a judge. The judge only sees what you put in the folder.\n\n## Formats you can use\n\n- **String**: one message, article, or passage.\n- **Object**: named fields or related records. This is usually the best pick.\n- **Array**: a list of messages or records, in order.\n\n## Rules of thumb\n\n- Keep facts in the state. Keep judgments in the questions.\n- Only send what the questions need. Extra stuff just adds noise.\n- Don't count on the model already knowing your private or up-to-date facts. Put those in the state.\n- Text only for now. No images, audio, or video.\n\n## Next\n\nRun the **Noul**, **Choice**, and **Score** labs.",
  "noul": "## Noul\n\n**Noul** asks a yes/no question.\n\nThe answer is **`noul`**, a number from 0 to 1. That number is the chance the answer is yes. 0 means definitely no. 1 means definitely yes.\n\nIt's like asking a friend for a gut check. They don't just say yes or no. They say, \"I'm about 80% sure that's a yes.\"\n\nYou can add optional `criteria.true` and `criteria.false` fields. They spell out what counts as a yes and what counts as a no.\n\nNoul answers have **no `confidence` field**. Use the probability itself, with thresholds you pick. ([Noul](https://docs.typesafe.ai/primitives/noul.md), [CORE](https://docs.typesafe.ai/primitives.md))\n\n### Watch out\n\nA Noul of 0.5 does not mean \"medium skill\". For graded levels like that, use **Score** with defined levels.\n\nOpen the [Noul lab](/labs/noul) for copy-paste curl and JS.\n\n### Next\n\n[Choice](/lessons/choice), then [Score](/lessons/score).",
  "choice": "## Choice\n\n**Choice** picks one option from a list you define. ([Choice](https://docs.typesafe.ai/primitives/choice.md))\n\nIt's like ordering at a restaurant. The menu has fixed options, and you pick one.\n\nYou get back three fields:\n\n- `choice`: the option that was picked.\n- `probabilities`: how likely each option was. They add up to about 1.\n- `confidence`: how sure the model is about the pick.\n\n### When your list might miss something\n\nAdd an `other` or `none` option. Use these when your list might not cover every input that comes in.\n\n### Lab bridge\n\nThis one is conceptual, not hands-on: AZMDR-style intent labels. See the [AZMDR routing lab](/labs/azmdr-routing). Do not touch the product repos.\n\n### Next\n\n[Score](/lessons/score), then confidence gating.",
  "score": "## Score\n\n**Score** rates something against an ordered rubric. The rubric needs at least two levels. ([Score](https://docs.typesafe.ai/primitives/score.md))\n\nIt's like a star rating. One star means weak. Five stars means excellent. A result can also land between levels, like a solid 3.5.\n\nYou get back four fields:\n\n- `score`: the rating. It can land between two levels.\n- `legend`: what the levels mean.\n- `probabilities`: how likely each level was.\n- `confidence`: how sure the model is.\n\n### The jaggedness caveat\n\nDon't use Score for exact numbers, counting, or dates. Jev is weak in those spots. Keep that math in your own code. ([Jaggedness](https://docs.typesafe.ai/model-jaggedness/jev-1.13.md))\n\nOpen the [Score lab](/labs/score).\n\n### Next\n\n[Confidence](/lessons/confidence).",
  "confidence": "## Confidence: a second signal\n\nEvery answer tells you *what*. Confidence tells you *whether* to act on it.\n\nOn **Choice** and **Score**, `confidence` is a number from 0 to 1. It comes from the shape of `probabilities`. When one option clearly wins, confidence is high. When the options sit close together, confidence is low. **Noul has no confidence** ([Confidence](https://docs.typesafe.ai/confidence.md)).\n\nThink of confidence like a weather forecast. A 95 percent chance of rain means grab the umbrella without looking outside. A 40 percent chance means check the sky first.\n\nWhat to do at each level:\n\n- **High confidence:** act automatically.\n- **Medium confidence:** confirm with a person, flag it, or gather more info.\n- **Low confidence:** do not act. Ask a human, ask for clarification, or pass it to another system.\n\nRaise the bar for destructive actions. A wrong auto-reply is annoying. A wrong refund is expensive. Start strict, then tune the thresholds on your own data ([Confidence-gated routing](https://docs.typesafe.ai/patterns/confidence-routing.md)).\n\nThe answer is the *what*. Confidence is the *whether*.\n\nOpen the [confidence lab](/labs/confidence).\n\n### Next\n[Fan-out](/lessons/fan-out).",
  "fan-out": "## Fan-out: many questions in one call\n\nPut many questions into **one** call. They all read the same state. Your code decides which answers matter ([Fan-out](https://docs.typesafe.ai/patterns/fan-out.md)).\n\nYou can even include speculative questions. Those are questions you might not need. If an answer turns out to be useless, your code just ignores it.\n\nIt's like a doctor's intake form. You answer ten questions on one sheet during one visit, not ten questions over ten separate appointments.\n\nThe state gets read once. The questions run in parallel. So one fan-out call beats a long line of single-question calls on cost and speed.\n\nThe parallel-questions cookbook tested this. Batching did not change the answers compared with one question per call. On their GDPR briefing example, it was about 12 times cheaper and about 10 times faster ([Parallel questions](https://docs.typesafe.ai/cookbooks/parallel_questions.md)).\n\nLab bridge: [newsletter triage](/labs/newsletter-triage). It asks an urgency Noul, a topic Choice, and a keep Noul, all in one call.\n\n### Next\n[Compose patterns](/lessons/patterns).",
  "patterns": "## Patterns you can combine\n\nThese patterns are like Lego bricks. Each brick is simple on its own. You snap them together in code to build something bigger.\n\n- **Intent routing.** Classify the request, then route it to fixed rules, a specialist LLM, or a human ([Intent routing](https://docs.typesafe.ai/patterns/intent-routing.md)).\n- **Composite scoring.** Run small, atomic Scores, then weight and combine them in code ([Composite scoring](https://docs.typesafe.ai/patterns/composite-scoring.md)).\n- **Confidence-gated routing.** The answer is the *what*. Confidence is the *whether* to act ([Confidence-gated routing](https://docs.typesafe.ai/patterns/confidence-routing.md)).\n- **Fan-out.** Many questions in one call ([Fan-out](https://docs.typesafe.ai/patterns/fan-out.md)).\n\nGoing further: instructions and criteria can also accept JSON structure ([Advanced](https://docs.typesafe.ai/primitives/advanced.md)).\n\n### Next\n[SDK labs](/lessons/sdk-labs), then pick a use-case.",
  "sdk-labs": "## JavaScript\n\n```bash\nnpm install @typesafe-ai/sdk\n```\n\nThe SDK is like a coffee machine. You don't grind the beans and boil the water yourself. You press a button. Same idea here: you call ready-made functions instead of writing raw HTTP requests.\n\n```ts\nimport { choice, noul, score, TypeSafeClient } from \"@typesafe-ai/sdk\";\n\nconst client = new TypeSafeClient();\nconst response = await client.systemOne({\n  state: { document: \"I was charged twice. Please fix this ASAP.\" },\n  questions: {\n    category: choice(\"What is this ticket about?\", {\n      billing: \"Payments, charges, refunds\",\n      technical: \"Bugs or integrations\",\n      other: \"Anything else\",\n    }),\n    urgent: noul(\"Does this convey urgency?\"),\n    frustration: score(\"How frustrated is the customer?\", [\n      \"Calm\",\n      \"Frustrated\",\n      \"Very angry\",\n    ]),\n  },\n});\n```\n\nThat call sends one support ticket and three questions. A Choice picks the category. A Noul checks urgency. A Score rates frustration. One call to System One, three answers back.\n\nDo **not** set `dangerouslyAllowBrowser: true` on public academy pages ([JS SDK](https://docs.typesafe.ai/sdk/javascript.md)).\n\n## Python\n\n```bash\npip install typesafe-sdk\n```\n\n```python\nfrom typesafe_sdk import Choice, Noul, Score, TypeSafeClient\n\nwith TypeSafeClient() as client:\n    response = client.system_one(\n        state={\"document\": \"I was charged twice. Please fix this ASAP.\"},\n        questions={\n            \"billing\": Noul(instructions=\"Is this ticket about billing?\"),\n            \"tone\": Choice(\n                instructions=\"What is the customer's tone?\",\n                criteria={\"calm\": None, \"frustrated\": None, \"angry\": None},\n            ),\n            \"urgency\": Score(\n                instructions=\"How urgent is this ticket?\",\n                criteria=[\"can wait\", \"this week\", \"today\"],\n            ),\n        },\n    )\n```\n\nSame idea in Python. One ticket, three questions, one `system_one` call.\n\nModels: prefer `jev-latest`. It's an alias, so the exact model behind it can change. Pin `jev-1.13.0` when you need stable thresholds ([Models](https://docs.typesafe.ai/models.md)). Optional extra: the [Agent skill](https://docs.typesafe.ai/agent-skill.md).\n\n### Next\n[Capstone labs](/lessons/project-labs) and the [use-case gallery](/use-cases).",
  "project-labs": "## Capstone labs: practice only\n\nThese labs are like a flight simulator. You practice the real moves, but you never fly the actual plane.\n\nRecreate the patterns here with copy-paste labs. **Do not touch** the AZMDR, newsletter, app-intake, or Fit Desk product repos. There are no live keys on this site.\n\nThe four themes:\n\n- **AZMDR-style request routing.** A Choice for the intent, plus confidence and a Noul for handing off to a human. Lab: [/labs/azmdr-routing](/labs/azmdr-routing)\n- **Newsletter triage.** Fan-out with several signals at once, plus human review when things are uncertain. Lab: [/labs/newsletter-triage](/labs/newsletter-triage)\n- **App intake moderation.** A Choice for the category, plus a Score for policy risk. Lab: [/labs/app-intake](/labs/app-intake)\n- **Fit Desk adherence.** A Noul for whether the user stuck with it, plus a Score for effort. Lab: [/labs/fit-desk](/labs/fit-desk)\n\nWant a stretch goal? Build from the public cookbooks: a RAG passage filter, LLM guardrails, or a citation check. See the [use-case gallery](/use-cases).\n\n### Next\n[Ship into a real repo](/ship).",
} as const;
